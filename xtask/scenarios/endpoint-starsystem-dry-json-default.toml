# endpoint-starsystem-dry-json-default
why = "GET /2.0/elite/starsystem, the market listing for one address — resolved but not sent (--dry-run), JSON output."
argv = ["markets", "--address", "3238296097059", "--dry-run", "--json"]

# --dry-run returns before `fetch` (ts:1228), so this one belongs to
# the `cli` suite: it runs without a server at all.
network = false
