why = "EDDN answering 202 with a body of OK. Success is `status === 200 && trim(body) === \"OK\"`, so 202 is a failure (R79) and the detail line carries the status and the clamped body."
argv = ["market", "--market-id", "3229009408", "--eddn"]

[[route]]
path = "/v2/market/3229009408"

[[route.reply]]
status = 200
payload-file = "payloads/ardent-market.json"
headers = [["Content-Type", "application/json"]]

[[route]]
path = "/2.0/elite/market/list"

[[route.reply]]
status = 200
encode = "capi"
payload-file = "payloads/market.json"

[[route]]
path = "/upload/"

[[route.reply]]
status = 202
payload = "OK"
