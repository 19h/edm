# fail-bad-size
why = "200 whose uncompressedsize is not a number: Number(\"twelve\") is NaN, which fails isSafeInteger (R10, R72)."
argv = ["market", "--market-id", "3229009408"]

[[route]]
path = "/2.0/elite/market/list"

[[route.reply]]
status = 200
encode = "capi"
payload-file = "payloads/market-small.json"
size-header = "twelve"
