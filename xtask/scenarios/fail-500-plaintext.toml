# fail-500-plaintext
why = "500 whose body is plain text: no nonce header, so the raw body is printed verbatim (ts:1263)."
argv = ["market", "--market-id", "3229009408"]

[[route]]
path = "/2.0/elite/market/list"

[[route.reply]]
status = 500
payload = "the market service is having a moment"
